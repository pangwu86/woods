#---------------------------------------------------------
# "@zozoh"
$ow  : ^(@)([a-z]+[a-z0-9]{3,})$
	${2} = String
#---------------------------------------------------------
# "@A:zozoh"     # admin
$admin       : ^(@A:)([a-z]+[a-z0-9]{3,})$
	${2} = String
#---------------------------------------------------------
# "@M:zozoh"     # member
$member      : ^(@M:)([a-z]+[a-z0-9]{3,})$
	${2} = String
#---------------------------------------------------------
# "@C:zozoh"     # contributor
$contributor : ^(@C:)([a-z]+[a-z0-9]{3,})$
	${2} = String
#---------------------------------------------------------
# "@W:zozoh"     # watcher
$watcher     : ^(@W:)([a-z]+[a-z0-9]{3,})$
	${2} = String
#---------------------------------------------------------
# "@AM:zozoh"    # admin + member
$roles     : ^(@)([AMCW]{2,})(:)([a-z]+[a-z0-9]{3,})$
	{r:'${2}',nm:'${4}'} = Json
#---------------------------------------------------------
# "^a.*"
$nm : ^([\^].+)$
	${1} = Regex
#---------------------------------------------------------
$d : ^([dD])([0-9]+)$
	${2} = INT
#---------------------------------------------------------
# ">nutzam"
$pa  : ^(>)([a-z0-9]+)$
    ${2} = String
#---------------------------------------------------------
# ">>nutzam"
$ans :: >>
    ${2} = String
#---------------------------------------------------------
# "->nutzam"
$frs :: ->
    ${2} = String
#---------------------------------------------------------
# "T(sin,org)"
$tp : ^(T[(])([A-Za-z, \t]*)([)])$
    ${2} = StringEnum
#---------------------------------------------------------
# "M(PUBLIC,PRIVATE)"
$md : ^(M[(])([A-Za-z, \t]*)([)])$
    ${2} = StringEnum
#---------------------------------------------------------
# "C(2013-09-10 12:34:15, 2013-09-11 13:45:12)"
# "C(2013-09-10, 2013-08-23)"
$ctm :: C
    ${2} = DateRegion
#---------------------------------------------------------
# "L[2013-09-11, 2013-07-25)"
$lm  :: L
    ${2} = DateRegion
#---------------------------------------------------------
# "#(酒店,餐饮)"
$lbs : ^(#[(])(.*)([)])$
    ${2} = StringEnum
#---------------------------------------------------------
# "lv(0)"
$lv  :: lv
    ${2} = IntRegion
#---------------------------------------------------------
$text : ^(.*)$
    ${1} = String

